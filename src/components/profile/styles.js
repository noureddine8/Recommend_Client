import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  container: {
    marginTop: 100,
    flex: 1,
  },
  box: {
    flex: 1,
  },
  list: { margin: 100 },
}));
