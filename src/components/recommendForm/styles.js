import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  container: {
    marginTop: 120,
    justifyContent: "center",
  },
  paper: { padding: 40, marginBottom: 50, marginTop: 50 },
}));
